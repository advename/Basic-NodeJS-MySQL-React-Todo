//Store keys seperatly, so we can disable this file from beeing uploaded to public gits
const key = {
  session: "The key used for sessions ",
  email: {
    user: "MEMAIL@gmail.com",
    password: "hello"
  }
};

module.exports = key;
